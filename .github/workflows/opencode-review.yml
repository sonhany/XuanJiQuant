name: opencode-review

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
      pull-requests: read
      issues: read

    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false

      - uses: anomalyco/opencode/github@latest
        env:
          OPENCODE_API_KEY: ${{ secrets.OPENCODE_API_KEY }}
        with:
          # OPENCODE_API_KEY 属 OpenCode Zen，必须用 opencode/ 前缀
          # 本账号实际启用的免费模型：opencode/nemotron-3-ultra-free、
          # opencode/nemotron-3.5-lightning-free、opencode/mimo-v2.5-free、
          # opencode/ling-3.0-flash-fin-free、opencode/muse-spark-1.3-contributor-free
          model: opencode/nemotron-3-ultra-free
          prompt: |
            审查这个 Pull Request：

            - 检查代码质量问题
            - 查找潜在的 bug
            - 检查是否引入安全风险或硬编码密钥
            - 提出改进建议

            用中文回复，按严重程度排序，控制在 300 字以内。
